---
'@solid-design-system/tokens': major
---

- Added fallback values from the ui-light theme to tailwind.css and components.css so applications can work without installing a theme;
- Added `dist/legacy-variables.css` as an optional fallback stylesheet option for accessing depracted variables.