---
'@solid-design-system/tokens': patch
---

Fixed token imports to preserve the color mappings defined in Figma. For example, `--sd-color-background-accent-550` now correctly references `accent-550` in `ui-light`.