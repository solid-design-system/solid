---
'@solid-design-system/components': minor
'@solid-design-system/docs': minor
---

Adding more screenshot tests related to the attribute `floating-label` for the components `sd-input`, `sd-select`, `sd-textarea` and `sd-datepicker`. Fixed small styling issues related to text sizing, label placement, and `sd-input` use cases for different types of inputs. Lasly, remove the 'sm' size when using floating labels, defaulting this size to be same as 'md' size.
