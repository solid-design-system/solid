---
'@solid-design-system/docs': patch
---

Improvements on `sd-datepicker` documentation:

- Updated `range` and `disabled-dates` stories to reflect new date conversion.
- Added new screenshot test for `min` and `max`.
