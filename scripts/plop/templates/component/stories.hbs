import '../../solid-components';
import { getDefaultArgs, renderDefaultStory } from '../../../scripts/storybook/helper';

export default {
  title: 'Components/{{ tag }}',
  component: '{{ tag }}',
  args: getDefaultArgs('{{ tag }}'),
};

export const Default = {
  render: (args: any) => {
    return renderDefaultStory('{{ tag }}', args);
  }
};
