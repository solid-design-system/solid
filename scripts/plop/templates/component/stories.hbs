import '../../solid-components';
import { storybookDefaults, storybookTemplates } from '../../../scripts/storybook/helper';

const { argTypes, args } = storybookDefaults('{{ tag }}');
const { defaultTemplate } = storybookTemplates('{{ tag }}');

export default {
  title: 'Components/{{ tag }}',
  component: '{{ tag }}',
  args,
  argTypes,
};


/**
 * Default: This shows {{ tag }} in its default state.
 */

export const Default = {
  render: (args: any) => {
    return defaultTemplate(args);
  }
};
