packages:
  # all packages in subdirs of packages/ and components/
  - 'packages/**/!(dist)/**'
  - 'demos/**/!(dist)/**'
  # exclude packages that are inside test directories
  - '!**/test/**'

minimumReleaseAge: 7200
minimumReleaseAgeExclude:
  # Renovate security update: svgo@4.1.0
  - svgo@4.1.0
