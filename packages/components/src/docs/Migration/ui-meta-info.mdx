# Migration Guide: From `ui-meta-info` to `sd-meta`

The new `sd-meta` is designed to replace the `ui-meta-info`. Rather than offering a complex component structure, they primarily focus on providing CSS styles for meta information.

## 💾 Slots

### ✨ New Slots

### ❌ Removed Slots

The following slots have been removed from the new sd-meta component:

1. [default]

<hr />

## 🎨 CSS Classes

### ✨ New CSS Classes

#### [sd-meta--size-sm]

The `sd-meta--size-sm` class defines the size of the meta element. You can use this option for a smaller text size to accomodate container/screen sizes.

#### [sd-meta--inverted]

This class inverts the meta element.

#### [sd-meta--pipe]

This class adds a pipe to the right of the meta element.

#### [sd-meta--light]

This class makes the sd-meta appear lighter.

<hr />
