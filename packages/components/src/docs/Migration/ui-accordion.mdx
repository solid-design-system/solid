# `ui-accordion`

The `sd-accordion-group` component will now replace the `ui-accordion`.

## 🔀 Updated attribute: `auto-close` to `close-others`

The `auto-close` attribute has been renamed to `close-others` to better describe its functionality.

## ✨ New `slot` component

A new `slot` component is introduced in `sd-accordion-group` which is where `sd-accordion` elements will be placed.

Please refer to the `sd-accordion-group` documentation for examples and more detailed information.
