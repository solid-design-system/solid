import '../../../../components/src/solid-components';
import { html } from 'lit-html';
import { storybookDefaults, storybookHelpers, storybookTemplate } from '../../../scripts/storybook/helper';

const { argTypes, parameters } = storybookDefaults('{{ tag }}');
const { overrideArgs } = storybookHelpers('{{ tag }}');
const { generateTemplate } = storybookTemplate('{{ tag }}');

/**
 *
 * Component description.
 *
 */

export default {
  title: 'Components/{{ tag }}/Screenshots: {{ tag }}',
  component: '{{ tag }}',
  tags: ['!autodocs'],
  parameters: {
    ...parameters,
    controls: { disable: true },
    design: {
      type: 'figma',
      url: ''
    }
  },
  args: overrideArgs([{}]),
  argTypes,
};

export const Default = {
  render: (args: any) => {
    return generateTemplate({ args });
  }
};
