import { Meta } from '@storybook/addon-docs/blocks';
import theme from 'tailwind-theme';
import cx from 'classix';

<Meta title="Packages/Tokens/Z-Index" />

## Z Index

<table>
  <tbody>
    {theme.utilities
      .filter(token => token.name.startsWith('z'))
      .map(token => ({ ...token, name: token.name.replace('z-', '') }))
      .map((token, i) => {
        return (
          <tr>
            <td>
              <div class="font-bold">sd-z-index-{token.name}</div>
              {token.declarations.map(declaration => (
                <div>
                  {declaration.prop}: {declaration.value}
                </div>
              ))}
            </td>
          </tr>
        );
      })}
  </tbody>
</table>
