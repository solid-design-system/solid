import { Meta } from '@storybook/addon-docs/blocks';
import theme from 'tailwind-theme';
import cx from 'classix';
import { getTokens } from '../utils.js';

<Meta title="Packages/Tokens/Preview/Z-Index" />

## Z Index

<table>
  <tbody>
    {getTokens('z', theme.utilities).map((token, i) => {
      return (
        <tr key={token.name}>
          <td>
            <div className="font-bold">sd-z-index-{token.name}</div>
            {token.declarations.map(declaration => (
              <div key={declaration.prop}>
                {declaration.prop}: {declaration.value}
              </div>
            ))}
          </td>
        </tr>
      );
    })}
  </tbody>
</table>
