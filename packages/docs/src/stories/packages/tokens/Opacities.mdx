import { Meta } from '@storybook/addon-docs/blocks';
import theme from 'tailwind-theme';
import cx from 'classix';
import { getTokens } from './utils.js';

<Meta title="Packages/Tokens/Opacities" />

## Opacities

<table>
  <tbody>
    {getTokens('opacity', theme.base, {
      sort: (a, b) => a.name.localeCompare(b.name, undefined, { numeric: true })
    }).map((token, i) => (
        <tr key={token.name}>
          <td>
            <div className="flex flex-col">
              <div className="font-bold">sd-opacity-{token.name}</div>
              <div>opacity: {token.value}</div>
            </div>
          </td>
          <td>
            <div className={`relative flex flex-col`}>
              {[...Array(12)].map((_, rowIndex) => (
                <div className="flex w-full" key={rowIndex}>
                  {[...Array(30)].map((_, cellIndex) => (
                    <div
                      className={`w-1 h-1 ${rowIndex % 2 === cellIndex % 2 ? 'bg-white' : 'bg-neutral-500'}`}
                      key={cellIndex}
                    />
                  ))}
                </div>
              ))}

              <div className={cx('bg-neutral-800 w-full h-full absolute inset-0 z-10', {
                '0': 'opacity-0',
                '5': 'opacity-5',
                '10': 'opacity-10',
                '20': 'opacity-20',
                '30': 'opacity-30',
                '40': 'opacity-40',
                '50': 'opacity-50',
                '55': 'opacity-55',
                '60': 'opacity-60',
                '70': 'opacity-70',
                '75': 'opacity-75',
                '80': 'opacity-80',
                '85': 'opacity-85',
                '90': 'opacity-90',
                '100': 'opacity-100',
              }[token.name])}/>
            </div>
          </td>
        </tr>
      ))}

  </tbody>
</table>
