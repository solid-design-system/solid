import { Meta } from '@storybook/addon-docs/blocks';
import theme from 'tailwind-theme';
import cx from 'classix';

<Meta title="Packages/Tokens/Shadows" />

## Shadow

<table>
  <tbody>
    {theme.base
      .filter(token => token.name.startsWith('shadow'))
      .map(token => ({ ...token, name: token.name.replace('shadow-', '') }))
      .map((token, i) => {
        const name = token.name === 'shadow' ? '' : `-${token.name}`;
        return (
          <tr>
            <td>
              <div class="flex flex-col">
                <div class="font-bold">sd-shadow{name}</div>
                <div>box-shadow: {token.value}</div>
              </div>
            </td>
            <td>
              <div
                class={cx(
                  'size-16',
                  {
                    shadow: 'shadow',
                    sm: 'shadow-sm'
                  }[token.name]
                )}
              ></div>
            </td>
          </tr>
        );
      })}
  </tbody>
</table>
