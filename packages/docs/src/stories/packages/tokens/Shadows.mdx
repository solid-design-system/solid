import { Meta } from '@storybook/addon-docs/blocks';
import theme from 'tailwind-theme';
import cx from 'classix';
import { getTokens } from './utils.js';

<Meta title="Packages/Tokens/Shadows" />

## Shadow

<table>
  <tbody>
    {getTokens('shadow', theme.base).map((token, i) => {
      const name = token.name === 'shadow' ? '' : `-${token.name}`;
      return (
        <tr key={token.name}>
          <td>
            <div className="flex flex-col">
              <div className="font-bold">sd-shadow{name}</div>
              <div>box-shadow: {token.value}</div>
            </div>
          </td>
          <td>
            <div
              className={cx(
                'size-16',
                {
                  shadow: 'shadow',
                  sm: 'shadow-sm'
                }[token.name]
              )}
            ></div>
          </td>
        </tr>
      );
    })}
  </tbody>
</table>
