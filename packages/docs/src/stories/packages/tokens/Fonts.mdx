import { Meta } from '@storybook/addon-docs/blocks';
import theme from 'tailwind-theme';
import cx from 'classix';

<Meta title="Packages/Tokens/Fonts" />

## Font Sizes

<table>
  <tbody>
    {theme.base
      .filter(token => token.name.startsWith('text-') && !token.name.includes('color'))
      .map(token => ({ ...token, name: token.name.replace('text-', '') }))
      .map((token, i) => {
        return (
          <tr>
            <td>
              <div class="font-bold">sd-font-size-{token.name}</div>
              <div>{token.value}</div>
            </td>
            <td>
              <div
                class={cx(
                  {
                    sm: '!text-sm',
                    base: '!text-base',
                    lg: '!text-lg',
                    xl: '!text-xl',
                    '2xl': '!text-2xl',
                    '3xl': '!text-3xl',
                    '4xl': '!text-4xl'
                  }[token.name]
                )}
              >
                Lorem ipsum dolor
              </div>
            </td>
          </tr>
        );
      })}
  </tbody>
</table>

## Font Weight

<table>
  <tbody>
    {theme.base
      .filter(token => token.name.startsWith('font-weight-'))
      .map(token => ({ ...token, name: token.name.replace('font-weight-', '') }))
      .map((token, i) => {
        return (
          <tr>
            <td>
              <div class="font-bold">sd-font-weight-{token.name}</div>
            </td>
            <td>
              <div
                class={cx(
                  {
                    normal: 'font-normal',
                    bold: 'font-bold'
                  }[token.name]
                )}
              >
                Lorem ipsum dolor
              </div>
            </td>
          </tr>
        );
      })}
  </tbody>
</table>
