import { Meta, Canvas } from '@storybook/blocks';
import { OverviewFormatter } from '../../Overview.jsx';

import * as SdCarouselStories from './carousel.stories';

export const storybookLink = '/?path=/docs/components-sd-carousel--docs';
export const figmaLibrary =
  'https://www.figma.com/design/VTztxQ5pWG7ARg8hCX6PfR/Solid-DS-%E2%80%93-Component-Library?node-id=3252-22063&p=f&t=B5mgTBjcWeycmzb6-0';
export const figmaDocs =
  'https://www.figma.com/design/YDktJcseQIIQbsuCpoKS4V/Component-Docs?node-id=2233-2414&t=pFHk5gNX1Hj4l15o-4';

export const content = `
# Carousel

Used to display an arbitrary number of content slides along a horizontal axis.

On touch devices, the slides can be moved by swiping.

<DefaultStory />

<DocumentationLinks storybookLink="${storybookLink}" figmaLibrary="${figmaLibrary}" figmaDocs="${figmaDocs}" />

#### Related Components

[sd-carousel-item](/?path=/docs/components-sd-carousel-item--docs)

#### Related Templates

[Carousel](/?path=/docs/templates-carousel--docs)

### Common Use Cases

- Highlight key features or promotions in a compact, rotating format.
- Create interactive galleries for users to browse through images or videos.
- Showcase testimonials or reviews to build credibility and trust.

### Usage Guidelines

#### Content and Style

- Ensure consistency in content and style across slides by following a similar layout and type of information.

#### Usage and Interaction

- Adapt the "slides per move" attribute to "slides per page" as needed, but keep the total number of available slides in mind: avoid moving more than one slide at a time if there are only a few slides.
- Refrain from placing critical content in a rotating format, as users may miss it.
- Enable swipe gestures to move slides on touch devices, while keeping the navigation arrows visible at all times.
- Avoid enabling autoplay if possible. If necessary, ensure there is an option to stop any automatic movement.

#### Background

- Use light background options like white, neutral-100, primary-100, or use a primary background when inverted.

### Accessibility Information

- Ensure that users can navigate through the carousel using the keyboard (e.g., Tab and Arrow keys).
- Navigation arrows are still displayed on touch devices to enable alternative interaction that complies with accessibility requirements.
- Show the pause/play control at all times.
- Ensure sufficient contrast between each slide’s content and its background.

#### ARIA Labels

- Use appropriate ARIA roles, such as ’aria-roledescription="carousel"’and ”aria-live” to indicate the state of the carousel and announce dynamic changes to screen reader users.
- Describe navigation controls and slides using ”aria-label” or ”aria-labelledby”.
- Add the role="region" attribute together with an unique aria-label whenever the carousel is an important landmark of the page.
- Visit <sd-link href="https://www.figma.com/design/YDktJcseQIIQbsuCpoKS4V/Component-Docs?node-id=10708-5011&t=Z1FxSGWkmXG5r5sw-4" target="_blank">Solid DS Best Practices for WCAG Compliance</sd-link> to learn more about our accessibility standards.`;

<Meta title="Components/sd-carousel/Overview" />

<OverviewFormatter story={SdCarouselStories.Default}>{content}</OverviewFormatter>
