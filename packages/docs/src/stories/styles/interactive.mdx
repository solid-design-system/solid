import { Meta, Canvas } from '@storybook/blocks';
import { OverviewFormatter } from '../../Overview.jsx';

import * as InteractiveStories from './interactive.stories';

export const storybookLink = '/?path=/docs/styles-sd-interactive--docs';
export const figmaLibrary =
  'https://www.figma.com/design/VTztxQ5pWG7ARg8hCX6PfR/Solid-DS-%E2%80%93-Component-Library?node-id=11907-281&p=f&t=B5mgTBjcWeycmzb6-0';
export const figmaDocs =
  'https://www.figma.com/design/YDktJcseQIIQbsuCpoKS4V/Component-Docs?node-id=2314-31823&node-type=canvas&t=ExKWUj3vUoucvcVt-0';

export const content = `
# Interactive

Used in interactive elements as a "quartery" button that has no paddings and no background.

<DefaultStory />

<DocumentationLinks storybookLink="${storybookLink}" figmaLibrary="${figmaLibrary}" figmaDocs="${figmaDocs}" />

## Related Templates

[Interactive](/?path=/docs/templates-interactive--docs)

## Common Use Cases

- Offer a minimal, “quartery” button style with no padding or background for inline interactions or icon-only triggers.
- Provide a text-like clickable element without the visual bulk of standard buttons.
- Create subtle controls within tables (e.g., arrows)

## Usage Guidelines

### Placement

- Avoid placing interactive elements too close together—users should easily distinguish and activate each link or button.
- Reserve this style for secondary or low-emphasis actions, rather than critical calls to action.

### Icons

- Display system icons before or after the interactive label, but not both.
- Use as an icon-only interactive element (e.g. expand and collapse arrows).

### Background and Icons

- Use with light background options of white, neutral-100 and primary-100 or if inverted with background primary.

## Accessibility Information

- Use the correct semantic element (e.g., <button> or <a>) for each interactive purpose to ensure screen readers announce the function accurately.
- If the interactive element is icon-only, include an aria-label describing its function (e.g., “Expand section”).
- Keep enough space around the “quartery” button so users can easily tap or click it, preventing accidental inputs on adjacent elements and ensuring a minimum target size of at least 44×44 px.
- Visit <sd-link href="https://www.figma.com/design/YDktJcseQIIQbsuCpoKS4V/Component-Docs?node-id=10708-5011&t=Z1FxSGWkmXG5r5sw-4" target="_blank">Solid DS Best Practices for WCAG Compliance</sd-link> to learn more about our accessibility standards.`;

<Meta title="Styles/sd-interactive/Overview" />

<OverviewFormatter story={InteractiveStories.Default}>{content}</OverviewFormatter>
