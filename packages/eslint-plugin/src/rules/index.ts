/**
 * DEV-NOTE: Please do not remove the comments `RULE IMPORTS` and `RULE EXPORTS`.
 * These are necessary to ensure that `pnpm plop` works and intended.
 */
import requiredSdIconLabel from './required-sd-icon-label/required-sd-icon-label.js';
// RULE IMPORTS

export default {
  'required-sd-icon-label': requiredSdIconLabel
  // RULE EXPORTS
};
