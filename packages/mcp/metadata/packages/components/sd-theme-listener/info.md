## Overview

`<sd-theme-listener>` — Theme listener is a utility that listens to theme changes and triggers an event.

## API

### Examples

Use the components tool by passing the args `component` and `example` for any of these combinations:

- component: sd-theme-listener, example: default

### Events

- event.sd-theme-change: Emitted when the sd-theme changes.
