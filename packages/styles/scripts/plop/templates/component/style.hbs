/**
 * Description of the style.
 * @name {{ tag }}
 * @status stable
 * @since 1.0
 * @boolean {{ tag }}--inverted Inverts the style.
 */

.{{ tag }} {
  @apply bg-primary-500 text-white;
  &--inverted {
    @apply bg-white text-primary-500;
  }
}
